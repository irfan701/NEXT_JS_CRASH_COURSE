import React from 'react';

const AboutLayout = ({children}) => {
    return (
        <div>
            <h1>I am About Layout </h1>
            {children}
        </div>
    );
};

export default AboutLayout;